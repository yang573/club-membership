const express = require('express');
const router = express.Router();

// Add members to the linkedin group
router.post('/members/update', function(req, res) {
  // TODO
});

module.exports = router;
